{% extends 'base.html.twig' %}

{% block title %}ExternalRating{% endblock %}

{% block body %}
    <h1>ExternalRating</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ external_rating.id }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ external_rating.value }}</td>
            </tr>
            <tr>
                <th>Votes</th>
                <td>{{ external_rating.votes }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_external_rating_index') }}">back to list</a>

    <a href="{{ path('app_external_rating_edit', {'id': external_rating.id}) }}">edit</a>

    {{ include('external_rating/_delete_form.html.twig') }}
{% endblock %}
