{% extends 'base.html.twig' %}

{% block title %}Edit Series{% endblock %}

{% block body %}
    <h1>Edit Series</h1>

    {{ include('series/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_season_index', {'idSeries' : series.id}) }}">Edit a Season</a>
    <a href="{{ path('app_actor_index', {'idSeries' : series.id}) }}">Edit a actors</a>
    <a href="{{ path('app_genre_index', {'idSeries' : series.id}) }}">Edit a genres</a>


    <a href="{{ path('app_series_index') }}">back to list</a>

    {{ include('series/_delete_form.html.twig') }}
{% endblock %}
